'use strict';

var React = require( 'react' );

var Homepage = React.createClass( {

	render: function () {

		return (
			<div className="">
				I'm the add page.
			</div>
		);
	}
} );

module.exports = Homepage;
